import { VERSION } from "./version.js";
import { composeConfigGet } from "./compose-config-get.js";
function config(octokit) {
  return {
    config: {
      async get(options) {
        return composeConfigGet(octokit, options);
      }
    }
  };
}
config.VERSION = VERSION;
import { composeConfigGet as composeConfigGet2 } from "./compose-config-get.js";
export {
  composeConfigGet2 as composeConfigGet,
  config
};
